const prisma = require("../../utils/database");

// create an artist profile and add them to an exhibition (POST, /artists)

const createArtistAddToExhibition = (req, res) =>{

};


module.exports = {
    createArtistAddToExhibition
};